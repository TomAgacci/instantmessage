#!/bin/sh
# (same full POSIX API server you approved earlier)
# — includes register/auth/reset/token/sign/heartbeat/lookup —
# — strictly POSIX, no Bashisms —
# — uses nc, awk, sed, grep, sha256sum, openssl —
